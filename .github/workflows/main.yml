name: DevSecOps Pipeline
on:
  push:
    branches:
      - main
      - vulnerable-app
      - secure-app
  pull_request:
    branches:
      - main
      - vulnerable-app
      - secure-app
permissions:
  contents: read
  packages: write

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Gitleaks (Secrets Scan)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --source . --redact --exit-code 0 --report-format json --report-path gitleaks-report.json
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"


      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Install pip-audit
        run: pip install pip-audit
      - name: Run Dependency Vulnerability Scan (SCA)
        run: pip-audit || true
      - name: Install Bandit (SAST)
        run: |
          pip install bandit

      - name: Run Bandit Security Scan
        run: |
          bandit -r . -f json -o bandit-report.json || true
      - name: Upload Bandit Report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: bandit-report.json

      - name: Run app syntax check
        run: |
          python -m py_compile app.py
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          args: >
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECTKEY }}
            -Dsonar.sources=.
            -Dsonar.exclusions=venv/**,__pycache__/**
            -Dsonar.c.file.suffixes=-
            -Dsonar.objc.file.suffixes=-
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  docker-build:
    runs-on: ubuntu-latest
    needs: build-test

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository }}:latest .
      - name: Generate SBOM with Syft
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh
          ./bin/syft scan "ghcr.io/${{ github.repository }}:latest" -o json > sbom.json
      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom-report
          path: sbom.json
      - name: Scan Docker image with trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ github.repository }}:latest
      - name: Scan Docker image with trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ github.repository }}:latest
          format: table
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL'
      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Push image to GHCR
        run: docker push ghcr.io/${{ github.repository }}:latest
  deploy:
    needs: docker-build
    runs-on: self-hosted
    steps:
      - name: Deploy locally
        run: |
           docker pull ghcr.io/sarrazouaghi09-star/devsecop-app:latest
           docker stop devsecop-app || true
           docker rm devsecop-app || true
           docker run -d -p 5000:5000 --name devsecop-app ghcr.io/sarrazouaghi09-star/devsecop-app:latest
  zap-scan:
    runs-on: ubuntu-latest
    needs: deploy

    steps:
      - name: OWASP ZAP Scan
        uses: zaproxy/action-baseline@v0.10.0
        with:
          target: 'http://192.168.1.171:5000'
      - name: Upload ZAP Report
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: report_html.html
